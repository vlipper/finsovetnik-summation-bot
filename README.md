# finsovetnik-summation-bot

## plan
- work with exceptions -> send message to personal chat
- send whole article html tag (article_tag = soup.find('article')) to LLM.
    It should not contain much more tokens, but includes some useful info
- add possibility to have a chat if user answered to bot's message
